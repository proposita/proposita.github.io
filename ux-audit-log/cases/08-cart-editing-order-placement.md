# Case 08 — Cart Editing / Order Placement

**Status:** Case closed, with desktop and tablet-width (768px) coverage confirmed and a mobile-width (375px) retest attempted but blocked, not completed — see the final Notes entry below. Built real, multi-style, multi-color, multi-size cart test data (PA1136 in Black and Dark Navy, plus PA1142 in Dark Navy on desktop; PA1136 in Black alone at tablet width) using the drawer only as a means to an end — the drawer's own mechanics were not re-tested here, per Case 07's coverage. The evaluation focused entirely on the "ORDER PREVIEW" cart page itself: quantity editing, line removal, and — the main focus — how the app manages multiple delivery dates (Add Delivery, Edit Delivery Dates, Move To Another Delivery, Duplicate, Remove Delivery), through to the checkout validation step. The tablet retest confirms every desktop finding and positive observation carries over unchanged at 768px, and additionally resolves Finding 2 (the per-color minimum-quantity indicator) from incomplete to fully confirmed — see the tablet Steps and Findings sections below.

**Retracted finding:** an earlier draft of this case reported, as Finding 1, that typing a date directly into the "Edit Delivery Dates" panel silently fails to save. That symptom was reproduced repeatedly through this session's own automated browser tooling (see the Steps section and the `14`–`17` screenshots), including on a dedicated re-verification pass run specifically because the UX Assessment Lead could not reproduce it. The UX Assessment Lead then tested the same interaction manually, multiple times, using a real keyboard, and reports that it behaves correctly: a validly-typed date is taken and saved, and an actually invalid date shows a clear "Invalid Date" message — neither of which this automated testing ever observed. Given a direct, repeated conflict between automated and manual results, and no way to demonstrate the symptom under real human interaction, **this is being retracted as a confirmed product defect** and treated instead as a probable artifact of how this session's browser-automation tooling injects text into the field (most likely bypassing the character-by-character key events and/or focus/blur cycle a real keyboard produces, which the field's validation logic may depend on). Full detail on what was actually observed, and why it's being retracted rather than just downgraded, is kept in the Notes section rather than the Findings section.

The two findings that remain stand independently of the above and were not affected by this tooling question: an asymmetry between "Move To Another Delivery" (which correctly blocks a move landing more than 30 days from an item's original date) and "Duplicate" (which can create the identical over-30-days situation via the same guided flow with no check at all, silently producing an invalid cart line that blocks checkout); and an incompletely-investigated per-color minimum-quantity indicator. Quantity editing, line removal, delivery removal, and the checkout Season Minimum validation all worked correctly and predictably, each backed by clear feedback or a confirmation dialog.

**Priority:** High overall, driven by Finding 1 (the Duplicate flow's own auto-suggested default routinely creates the very invalid state Move's validation exists to prevent, with no front-end warning until checkout) — confirmed to reproduce identically at tablet width. Finding 2 (per-line minimum-quantity indicator) is Low now that the tablet retest has confirmed it is advisory-only and does not block checkout — see its own priority note, updated below. The retracted delivery-date-typing symptom is not scored, since it could not be confirmed as a real, user-facing defect.

## Scope

- **Area:** Cognitive walkthrough of the "Cart Editing / Order Placement" journey: building cart test data across different styles, sizes, and colors; then, on the cart ("ORDER PREVIEW") page itself, testing quantity increase/decrease, line removal, and — as the primary focus — every delivery-date management action available from the delivery group's and each line's "⋮" menus (Add Delivery, Edit Delivery Dates, Move To Another Delivery, Duplicate, Remove Delivery); then reaching the checkout/confirmation step and observing what validation fires there. Per explicit scope, the right-side add-to-cart drawer's own functionality (quantity limits, per-size/per-color entry, guard dialogs, accessibility) was not re-tested — it was used only as a means of getting real data into the cart, and is already fully covered by Case 07.
- **Screen:** `/reports/3771` (the Fall 2026 linesheet report) → the style detail drawer for "3/4 Sleeve Boatneck" (PA1136) and a second style, PA1142 → `/my-account/cart` (the "ORDER PREVIEW" cart screen), including its "Edit Delivery Dates" side panel → the "Submit Orders" checkout validation dialog.
- **Interaction boundary:** Tested using the user's own real, already-authenticated Chrome session. Added real quantities across two styles, three color lines, and (temporarily, to test delivery management) a second delivery group, then exercised the cart's own editing, delivery-management, and checkout-validation controls directly. Per explicit user instruction, "Submit Orders" was clicked once, deliberately, as an approved checkpoint to observe the checkout validation screen — but only after asking and receiving explicit confirmation first, and never actually confirmed/finalized: the resulting "Opening Order Below Season Minimum" dialog was exited via "Go back," with no order placed. All test-added lines and the extra delivery group were removed afterward, restoring the cart to empty, consistent with this audit's standing practice of leaving the shared test account as it was found.
- **Session:** Eighth case of this audit round, now closed. Desktop pass used the user's own real Chrome session (Claude in Chrome). The tablet retest (768px) was run separately, per explicit request, using this session's built-in browser pane rather than Claude in Chrome (which had gone offline after an accidental logout during the desktop Finding 1 re-verification) — see the tablet Steps section for the coordinate-mapping detail this switch introduced. A mobile retest (375px) was attempted afterward, once a customer was selected, first via this session's built-in browser directly and then via a collaborative workflow with the UX Assessment Lead performing clicks manually — both approaches hit the same unresponsive click, including on the UX Assessment Lead's own direct click, so the mobile retest was abandoned and the case closed with desktop and tablet coverage only. See the final Notes entry for the full account.

## Steps — building cart test data across styles, colors, and sizes

- Opened the Fall 2026 linesheet report (`/reports/3771`) and used the "3/4 Sleeve Boatneck" (PA1136) drawer to add Black (S 3 / M 5 / L 2 / XL 4 — later adjusted, see below) and Dark Navy, then added a second style, PA1142, in Dark Navy — deliberately spreading test data across two styles, three color lines, and a mix of sizes and quantities, so the cart page itself would have enough real structure to exercise its own editing and delivery-grouping behavior meaningfully. The drawer's own entry, quantity, and add-to-cart mechanics were not re-examined here — see Case 07.
- Landed on `/my-account/cart`, confirming all three color lines appeared correctly grouped under one delivery header ("Fall 2026: September 9/30 X-Warehouse"), each with its own per-line "⋮" menu (Move To Another Delivery, Duplicate, Edit Quantity, Remove Color) and the delivery group's own "⋮" menu (Add Delivery, Edit Delivery Dates).

## Steps — Edit Delivery Dates: typed input vs. calendar selection

**Note added after this case was first written: the typed-input symptom described in this section could not be reproduced under real, manual keyboard input by the UX Assessment Lead, and has been retracted as a confirmed product defect — see the Notes section for the full account. The steps below are kept as an accurate record of what this session's automated browser testing did and observed, not as confirmed end-user-facing behavior.**

- Opened the delivery group's "⋮" menu → "Edit Delivery Dates": a right-side panel opened with "Start date" and "X-Warehouse date" fields, each prefilled with the delivery's current committed values (Start 09/09/2026, X-Warehouse 09/30/2026) and each with a calendar-icon toggle plus an inline bsDatepicker dropdown that opens on clicking directly in the text field. Screenshot: `01-edit-delivery-dates-panel-defaults.jpg`.
- Typed an invalid date directly into the Start Date field (`10/15/2026` typed over the field while a calendar dropdown for September was still open and showing day 9 highlighted underneath). The field visibly displayed the typed text with no inline validation error of any kind. Screenshot: `02-typed-invalid-date-accepted-no-validation.jpg`.
- Clicked the panel's "Save" button (located and clicked via its real DOM position, not a stale screenshot coordinate, after discovering that layout shifts from the open calendar dropdown could otherwise cause a click to land on a calendar day instead). The panel closed and the cart page's delivery header initially appeared unchanged — still reading "September 9/30." Screenshot: `03-cart-after-failed-save-header-unchanged.jpg`.
- Reopened "Edit Delivery Dates" on the same delivery to check what had actually been saved: both fields showed the original, unmodified values (09/09/2026 and 09/30/2026) — confirming the typed date was never actually committed, despite having been accepted into the field and apparently submitted via Save with no error shown anywhere in the process. Screenshot: `04-reopened-panel-confirms-silent-revert.jpg`.
- Inspected the underlying network traffic during this Save via `read_network_requests`. **Correction, confirmed on a follow-up re-verification pass (see below): no `PATCH` request is sent to the server at all when Save is clicked after typing.** The original write-up of this finding stated the endpoint returns a misleading HTTP 200 on a no-op save; that description was inaccurate. The failure happens earlier and entirely client-side — Save's click handler never calls the API in this case, so there is no server response, misleading or otherwise, to inspect.
- Repeated the same field-then-Save sequence with a case where the typed value was itself a plausible, in-range date (not obviously invalid), to rule out this being purely a garbage-input rejection: the same silent no-op pattern occurred — the field accepted the typed text, no network request fired, and reopening the panel showed the original value unchanged. Typing into these fields, in other words, does not reliably update the app's real underlying data under any value tested, valid-looking or not.
- **Re-verification pass, run specifically because the UX Assessment Lead could not reproduce this finding:** repeated the exact sequence live — open "Edit Delivery Dates," triple-click Start Date, type a fresh date directly (`09/16/2026`, then again with `09/17/2026`), click Save. Screenshot: `14-reverify-typed-date-day9-still-selected.jpg` (field shows `09/17/2026` typed, while the calendar dropdown open underneath still shows day 9 — the true committed date — as the selected day, not day 17). In both runs, `read_network_requests` showed zero requests to `/api/carts/deliveries/*` (confirmed by clearing the network log immediately before each Save click), and reopening the panel afterward showed the original Start date (`09/09/2026`) unchanged both times. Screenshot: `16-reverify-reopened-panel-still-shows-original-date.jpg`. Inspecting the field's own DOM state at the moment of typing made the cause visible directly: the input's Angular classes read `ng-touched ng-dirty ng-invalid` — the framework considers the typed value invalid — while its computed border color stayed the same neutral gray as a valid field (`rgb(198,198,198)`, no red, no error text anywhere nearby). Save's handler checks form validity before calling the API and silently declines to submit when it's invalid, which is why nothing reaches the network layer and nothing on screen indicates a problem. In one of the two runs, clicking Save didn't even close the panel — it just sat there, and the field's displayed text snapped from the just-typed `09/17/2026` to `09/16/2026` (a stale, previously-typed value from the prior run, not the true committed `09/09/2026` and not what was just typed) — a further, separate sign of how disconnected the visible input text is from any real, single source of truth. Screenshot: `15-reverify-save-clicked-panel-stays-open-stale-value.jpg`. For contrast, the working calendar-click path was re-run in this same session too — select a real day, click Save, reopen — and correctly persisted (`09/14/2026`, confirmed on reopen). Screenshot: `17-reverify-calendar-click-date-persists-correctly.jpg`.
- Also tested closing the panel via `Tab`, `Enter`, and `Escape` after typing (rather than clicking Save): `Tab` and `Enter` both closed the entire panel outright, discarding the typed text with no save attempt at all. `Escape` reverted the field's displayed value — but to an earlier, accidentally-selected calendar date from a prior step in this same test session, not to the actual last-saved value, confirming the field's displayed text and the app's real committed value can drift apart in more than one way.
- By contrast, selecting a date via an actual calendar-cell click (a full `mousedown`/`mouseup`/`click` sequence on the inner day element, discovered to be the only reliable interaction after direct DOM value-injection and the `computer` tool's synthetic typing both failed to update Angular's real bound form state) followed by Save **did** persist correctly: the cart's delivery header updated to the new date immediately, and reopening the panel confirmed the new value was retained. Screenshot: `06-valid-calendar-date-change-persists.jpg`.
- Separately confirmed a genuine positive business rule while exploring the calendar itself: the Start Date calendar disables Saturdays and Sundays outright (checked via each day cell's enabled/disabled state across a full visible month), preventing a delivery window from ever starting on a weekend.

## Steps — Add Delivery, Move To Another Delivery, and Duplicate

- Used the delivery group's "⋮" menu → "Add Delivery": a second, initially empty delivery group appeared, with dates auto-suggested as the day after the first delivery's X-Warehouse date, spanning 15 days (prior delivery ends 9/30 → new delivery defaulted to Start 9/30 / X-Warehouse 10/15). Screenshot: `05-add-delivery-creates-new-empty-group.jpg`.
- Tested "Move To Another Delivery" on one color line (dragging its row onto the new, empty delivery's drop zone): blocked immediately with a red toast reading **"The item cannot be moved to a delivery date greater than 30 days from its original date."** Screenshot: `07-move-to-delivery-blocked-30-day-window.jpg`.
- Reproduced the identical block via the guided, non-drag path instead: per-color "⋮" menu → "Move To Another Delivery" → "Add Delivery" (the only delivery-target option available with just one existing delivery) — this path's own auto-suggested default (also computed 36 days out from the original delivery, consistent with the drag-and-drop attempt) fails the same 30-day rule on the very first try. Screenshot: `08-move-guided-flow-immediately-fails.jpg`. In other words, the guided flow's own default date guarantees failure of its own validation rule unless the user manually overrides the suggested date first — see Finding 1.
- Tested the same guided path but with "Duplicate" instead of "Move To Another Delivery," to see whether the equivalent restriction applied there too: per-color "⋮" menu → "Duplicate" → "Add Delivery," using the same auto-suggested 36-days-out default. Unlike Move, this **succeeded with no block, warning, or toast of any kind** — a new delivery group and a duplicated color line were created immediately. Screenshot: `11-duplicate-creates-second-delivery-line.jpg`.
- The only indication anything was wrong appeared back on the main cart list: a small orange "!" warning icon on the newly duplicated line, whose tooltip read: **"This item cannot belong to the delivery you are currently in because the item's date is prior to the delivery date. Please relocate the item to continue."** Screenshot: `12-duplicate-blocks-checkout-hidden-tooltip.jpg`. Confirmed this state also disabled "Submit Orders" entirely (checked directly via the button's `disabled` attribute) — the invalid state isn't just cosmetic, it silently blocks checkout with no explanation on the cart page itself beyond this one small icon.
- Attempted a second, cleaner reproduction of a working (non-blocked) drag-and-drop Move, to have positive-path evidence alongside the blocked one. Across three separate attempts, the X-Warehouse date calendar's range of enabled days appeared to stay anchored near the current date rather than reactively updating after a Start Date change made earlier in the same panel/reopen cycle — flagged here with lower confidence, since it may be an artifact of this session's own JS-driven interaction approach rather than genuine end-user-facing behavior, and was not pursued to a fully conclusive resolution (see Notes).

## Steps — quantity editing and line removal

- Used "Edit Quantity" on the PA1136 Black line: reopened the same right-side drawer used for adding items, pre-filled correctly with the row's current per-size quantities. Changed the S/M split from 2/3 to 3/2 (5 units total, $190.00 unchanged) — the action button correctly relabeled from "SELECT QUANTITY" (disabled) to "UPDATE QUANTITY" (enabled) the moment a value changed, and clicking it updated the cart row immediately, confirmed still correct after closing and reopening the drawer. Screenshot: `09-edit-quantity-persists-to-cart-row.jpg`.
- Used "Remove Color" on one of the test lines: a native confirmation dialog appeared ("Remove item — Are you sure you want to remove this item from your order?"), and confirming it removed the line correctly. Screenshot: `10-remove-color-confirmation-dialog.jpg`.
- Confirmed "Remove Color" and "Remove Delivery" both work correctly even on an item/delivery already in an invalid or errored state — used "Remove Color" directly on the invalid duplicated PA1142 line from the previous section, with no special handling needed; the confirmation dialog and removal both behaved identically to a normal, valid line.
- Noted, without deep-diving further, a separate per-color minimum-order-quantity indicator: a small orange "!" icon near a style's price range in the cart, tied to a circled quantity value on a color row below some threshold (observed at "3" units on both PA1136 Dark Navy and PA1142 Dark Navy) — seen consistently enough across two unrelated styles to read as a general business rule rather than something style-specific. See Finding 2.

## Steps — reaching checkout

- With the cart back in a clean, single-delivery, all-valid state, checked in with the UX Assessment Lead on how far to go at the confirmation step before proceeding further, per the standing rule that "Submit Orders" is never clicked without an explicit, specific go-ahead. Received explicit approval to click "Submit Orders" and observe the resulting screen, but to cancel out rather than confirm/finalize anything.
- Clicked "Submit Orders": triggered an **"Opening Order Below Season Minimum"** dialog reading "Your opening order is almost complete. Your seasonal order is below the Season Minimum. Add $2,702.00 to meet the minimum. You may still submit your order, but opening orders below the Season Minimum require sales management approval before they are confirmed," with "Go back" and "Continue" buttons. Screenshot: `13-checkout-season-minimum-validation.jpg`.
- Attempted to click "Continue" (within the scope of the approval already given, intending only to observe the next screen and stop there without confirming) — this action was blocked by this session's own automated safety guardrail before it reached the page, with guidance not to attempt any workaround. No workaround was attempted; "Go back" was used instead to exit the dialog with no state change. See Notes for the testing-methodology implication of this stopping point.

## Steps — cleanup

- Removed all test-added lines (PA1136 Black, PA1136 Dark Navy, PA1142 Dark Navy, and the invalid duplicated PA1142 Dark Navy line) via "Remove Color," each behind its own confirmation dialog, and removed the extra, now-empty second delivery group via "Remove Delivery" (also behind its own confirmation dialog).
- Confirmed the cart's delivery group was empty at the end of testing. "Submit Orders" was never actually confirmed/finalized at any point in this case — no real order was placed.

## Steps — retesting at tablet width (768px)

- Set the built-in browser pane to the 768×1024 tablet preset and confirmed the authenticated session (Alan Jalife / Bella Jules, no login needed). Navigated to the same "Fall 2026" report (`/reports/3771`) used on desktop, added PA1136 in Black (S 2) to the cart, and confirmed the cart's delivery header read "Fall 2026: September 9/30 X-Warehouse" — matching desktop exactly. **Methodology note:** an earlier attempt at this step used `/reports/all`, which auto-redirects to a different collection ("1X1 Rib" under a generic "OCT" grouping) with a different delivery header entirely; that test data was discarded before any findings were drawn from it, once the mismatch with desktop's report was noticed.
- **Edit Delivery Dates, calendar-click path:** opened the delivery group's "⋮" → "Edit Delivery Dates" and changed the X-Warehouse date via calendar click. The first click attempt appeared to do nothing — the field stayed at its old value with no visible change. Inspecting the calendar's DOM directly showed why: in this field specifically, every day in the current month is disabled except the currently-selected one (here, day 30) — a legitimate business rule (confirmed separately below), not a stuck click. Once retargeted at an actually-enabled day, a single real click set a light "hover/preview" highlight on that day, and a second click on the same day committed it, updated the field, and closed the calendar — the change then persisted correctly to the cart's delivery header on reopening the panel, exactly matching desktop's confirmed (non-retracted) calendar-click behavior. Reverted the date back to 9/30 afterward to keep testing on a clean baseline.
- **Coordinate-mapping detail, tooling-only, not a product finding:** while chasing the "first click does nothing" symptom above, discovered that this session's built-in-browser `computer` tool's screenshot-frame coordinates do not map to page CSS pixels by the expected screenshot-to-viewport ratio (800×1066 screenshot vs. a nominal 768×1024 viewport, i.e. ~1.042×) — the real, empirically-measured ratio here was ~1.094×, confirmed by attaching a real `click` event listener to `document` and reading back the actual `clientX`/`clientY` a computer-tool click landed at. Using the wrong ratio caused several clicks to land one calendar cell away from the intended target, at first looking like the same silent-failure pattern as the retracted desktop Finding 1. Recalibrating against the measured ratio resolved it immediately and every subsequent calendar interaction worked as expected. This is recorded here as a caution for future tablet/mobile retests using this specific browser tool — verify the actual screenshot-to-CSS-pixel ratio empirically before trusting coordinate math derived from the nominal viewport size, rather than assuming it matches the preset's nominal dimensions.
- **Positive business rule, confirmed independently at tablet width:** the X-Warehouse date field disables every day of the current month except the one already committed, while enabling weekdays (but not weekends) in the following month once navigated to — e.g., with 9/30 committed, Oct 1–2 (Thu–Fri) were enabled but Oct 3–4 (Sat–Sun) were not, and every day 1–29 of the current month was disabled outright. This is a stricter, date-field-specific version of the Saturday/Sunday rule already confirmed on desktop for the Start Date field, and appears to be working as intended.
- Selecting an X-Warehouse date later than the item's own ship-readiness window (e.g., 10/8, against the item's "7/29X" tag) correctly surfaced the same warning seen conceptually on desktop: an orange "!" icon reading, in full, **"This item cannot belong to the delivery you are currently in because the item's date is prior to the delivery date. Please relocate the item to continue."** Reverted to 9/30 to clear it.
- **"Duplicate" vs. "Move To Another Delivery," guided flow:** per-color "⋮" → "Duplicate" → "Add Delivery" (the only target with one existing delivery) used an auto-suggested default of 15 days out (9/30 → 10/15) — notably not the 36-days-out default recorded on desktop for this same guided-flow path, though the practical outcome was identical: **Duplicate succeeded immediately, with no block, warning, or toast**, creating a second delivery group and a duplicated line that then showed the same "item's date is prior to the delivery date" icon and confirmed-disabled "Submit Orders" as desktop's Finding 1 described. Cleaned this up (Remove Style, then Remove Delivery on the now-empty second group) and re-ran the identical guided path with **"Move To Another Delivery"** instead, using the same 10/15 default: this time it was **blocked immediately** with a red toast reading "The item cannot be moved to a delivery date greater than 30 days from its original date." Because both actions here used the exact same 15-day-out default date and the same starting state, this is if anything a cleaner reproduction of the Move/Duplicate asymmetry than desktop's own 36-days-out run — confirming the 30-day check is measured against the item's own original/expected date (not the gap between delivery dates, since 15 days is well inside a 30-day window measured delivery-to-delivery) and that Duplicate simply never runs the equivalent check at all, regardless of the offset used. See Finding 1.
- **Edit Quantity and the per-color minimum indicator, resolved:** reopened "Edit Quantity" on the PA1136 Black line (pre-filled correctly with S=2) and stepped the total up one unit at a time, checking the cart's orange "!" icon and its tooltip after each save: at **3 units** the icon was present, with tooltip text **"One or more colors are below the required color minimum."**; at **4 units** the icon was gone. This pins the threshold at exactly **4 units** per color — the number desktop's Finding 2 left unconfirmed. Also confirmed directly (via the Submit button's `disabled` property, not just visually) that this state never disables "Submit Orders" at any point — it is advisory-only, unlike Finding 1's icon. This fully resolves Finding 2 from "incomplete" to a confirmed, low-priority positive-with-a-caveat; see the updated Finding 2 below.
- **Remove Color / Remove Delivery:** both actions showed the same native-style confirmation dialogs seen on desktop ("Remove item — Are you sure you want to remove this item from your order?" and "Remove delivery — Are you sure you want to remove this delivery from your order?"), rendered correctly within the tablet layout, and both completed correctly when confirmed.
- **Checkout checkpoint:** with the cart back in a clean, single-line, valid state ($152.00, 4 units), checked in explicitly before touching "Submit Orders" again, per the standing rule that approval is per-case and does not carry over automatically from the desktop exception. Received explicit approval to click Submit Orders and exit via the equivalent of "Go back" without confirming anything. Clicking "Submit Orders" surfaced a **different** dialog than desktop's "Opening Order Below Season Minimum" step: a **"SELECT CUSTOMER — Customer must be selected for this action"** prompt with "CANCEL" and "SELECT" buttons. This is very likely a session/account-context difference (no active customer selected in this particular browser tab's session) rather than a tablet-specific behavior, and was not investigated further — exited immediately via "CANCEL" with no state change, consistent with the scope of the approval given (observe, don't proceed into an unreviewed flow). Recorded here as an open question rather than a finding: whether this same prompt would also appear on desktop with no customer selected, or whether it's specific to how this session's built-in-browser tab reached the cart, is unconfirmed.
- **Cleanup:** removed the test line (Remove Color) and confirmed the cart returned to its empty starting state, consistent with this audit's standing practice.

## Findings

### Finding 1 — "Duplicate"'s guided flow can create an invalid, checkout-blocking cart state with no front-end warning, unlike the equivalent "Move" flow

"Move To Another Delivery" and "Duplicate" share the same guided per-color "⋮" menu path when only one delivery exists: choosing either one offers "Add Delivery" as the only target, which auto-suggests a new delivery date 36 days out from the original. For "Move," this default immediately fails the app's own 30-day move-eligibility rule, with a clear red toast explaining why — confirmed via both the guided menu path and an equivalent drag-and-drop attempt, so the enforcement is real and consistent, not just a toast tied to drag interactions specifically.

"Duplicate," walked through the identical guided path with the identical 36-days-out default, has no equivalent check. It succeeds outright — creating a new delivery group and a duplicated color line with no block, warning, or confirmation of any kind. The only trace that something is wrong appears back on the main cart list, as a small orange "!" icon on the affected line, whose tooltip explains the item's date conflicts with its delivery's date. This state was confirmed to disable "Submit Orders" entirely — so the practical consequence isn't cosmetic, it silently prevents checkout — but nothing on the page proactively tells the user that, or why, unless they notice and hover a small icon among what could be a much longer cart.

Because both actions route through the exact same guided flow with the exact same auto-suggested default date, this isn't an edge case reachable only through unusual input — it's the natural result of following Duplicate's own suggested default without changing it, which is very plausibly what most users would do the first time.

**Confirmed at tablet width (768px):** re-ran both guided flows using the identical 15-days-out default date for both actions (rather than desktop's 36-days-out default) — Move was blocked immediately with the same red toast, Duplicate succeeded with no block of any kind. Same asymmetry, same silent checkout-blocking consequence, different specific offset — reinforcing that the missing check in Duplicate isn't tied to any particular date gap.

**Heuristic relevance:** Consistency and standards (two visually and structurally parallel actions, reached through the same menu shape and the same default-date logic, enforce completely different levels of validation); error prevention (Duplicate lets an invalid state get created at all, rather than preventing it at the point of action the way Move does); visibility of system status (the checkout-blocking consequence is real and immediate, but the only feedback is a small icon that must be actively discovered).

**Suggested direction:** Apply the same 30-day-window validation already implemented for "Move" to "Duplicate"'s equivalent guided flow, ideally by having both actions share the same underlying date-eligibility check rather than maintaining two separate implementations that can drift, as they clearly already have. At minimum, surface a toast identical to Move's immediately when Duplicate's guided flow would create the same invalid relationship, rather than allowing the action to complete silently and surfacing the problem only via a small icon discovered later.

**Priority note:** High. This directly parallels a rule the app already enforces correctly elsewhere, so the fix is largely a matter of applying existing logic consistently rather than designing something new — and until then, it's a checkout-blocking dead end that's easy to trigger by simply accepting Duplicate's own suggested default.

### Finding 2 — A per-color minimum-quantity indicator (now confirmed: threshold 4, advisory-only)

An orange "!" icon appears near a style's price range in the cart, tied to a circled value on a specific color row, whenever that color's total quantity falls below the required minimum. Desktop testing observed this at "3" units on two unrelated styles (PA1136 Dark Navy and PA1142 Dark Navy) but left the exact tooltip text and threshold unconfirmed. The tablet retest (768px) resolved both: stepping the PA1136 Black line's quantity up one unit at a time, the icon was present at 3 units and gone at 4, pinning the **threshold at exactly 4 units per color**. Its tooltip reads, in full: **"One or more colors are below the required color minimum."** Checked directly against the "Submit Orders" button's `disabled` property (not just visually) at 3 units: the button remained enabled throughout — this indicator is **advisory only** and does not block checkout, unlike Finding 1's icon.

**Heuristic relevance:** Visibility of system status — the business rule (a 4-unit-per-color minimum) is now clearly confirmed and its messaging, while generic ("one or more colors," rather than naming which one when there are several lines), correctly and non-disruptively surfaces a real constraint without blocking the user's ability to proceed.

**Suggested direction:** Minor: consider having the tooltip or icon identify which specific color(s) are under the minimum when a style has multiple color rows, rather than the generic "one or more colors" phrasing — a small clarity improvement, not a defect fix.

**Priority note:** Low. Originally flagged Medium on desktop specifically because it was unconfirmed whether this indicator blocked checkout; now that tablet testing has confirmed it does not, and that its threshold and messaging are both clear and correct, this is closer to a positive observation than an outstanding finding — kept as a Finding rather than moved to Positive observations only because the "which color" ambiguity noted above is a genuine, if minor, opportunity.

## Positive observations

- The Start Date calendar correctly disables Saturdays and Sundays outright, preventing a delivery window from ever being set to start on a weekend — a genuine, correctly-implemented business rule.
- "Add Delivery"'s auto-suggested default (the day after the prior delivery's X-Warehouse date, spanning 15 days) is a sensible, low-friction default for the common case of adding a straightforward next delivery window.
- "Move To Another Delivery" enforces its 30-day window rule consistently and clearly across two different interaction paths tested (drag-and-drop and the guided per-color menu), with the same specific, understandable toast message both times.
- "Edit Quantity" round-trips correctly: it reopens pre-filled with the row's real saved per-size values, correctly toggles its action button's label and enabled state only once something actually changes, and the update persists to the cart row immediately and durably.
- "Remove Color" and "Remove Delivery" both show clear native confirmation dialogs before acting, and both work correctly and predictably even when the specific item or delivery being removed is already in an invalid/errored state — a good, consistent safety net.
- The checkout "Opening Order Below Season Minimum" validation is clear, specific (it states the exact dollar amount needed to meet the minimum), and correctly explains the consequence (sales-management approval required) rather than just blocking with no context.
- When a real, valid calendar-cell date selection is made in "Edit Delivery Dates," the Save flow works exactly as expected — the change is immediate, visible on the cart header, and durable across reopening the panel. Per the UX Assessment Lead's own manual testing, typing a date directly also works correctly under real keyboard input: a valid typed date saves, and an invalid one shows a clear "Invalid Date" message — see Notes for the discrepancy between this and what this session's automated testing observed.
- **Confirmed at tablet width (768px):** every desktop finding and positive observation in this list carries over unchanged at 768px — delivery-date calendar-click editing, the Move/Duplicate asymmetry (Finding 1), Edit Quantity's round-trip behavior, and both confirmation dialogs (Remove Color, Remove Delivery) all behaved identically. The X-Warehouse date field additionally disables every day of its own current month except the already-committed one, on top of the weekend-blocking rule already confirmed for Start Date — a stricter but sensible extension of the same pattern.

## Notes / follow-up needed

- **Retracted: "Edit Delivery Dates" typed-input save failure.** This case originally reported, as a High-priority Finding 1, that typing a date directly into the Start Date or X-Warehouse Date field and clicking Save silently fails to persist the change. That symptom was reproduced multiple times through this session's own browser automation (screenshots `02`–`04`, `06`, and `14`–`17`), including a dedicated re-verification pass with DOM-level evidence (the field picking up Angular's `ng-invalid` class with no visible sign of it, and `read_network_requests` confirming no `PATCH` request is sent on that path). That re-verification pass was itself prompted by the UX Assessment Lead reporting they could not reproduce the symptom on a first attempt. Rather than settling the question, the automated re-check reproduced the same symptom again — at which point the UX Assessment Lead tested manually, more than once, with a real keyboard, and reports the field behaves correctly: a validly-typed date saves, and an actually invalid one shows a clear "Invalid Date" message. Neither of those behaviors was ever observed through this session's automated testing. Given that direct, repeated conflict, and no way to make the symptom appear under real human interaction, this is retracted as a confirmed product defect rather than merely downgraded. The most likely explanation is that this session's browser-automation tool injects text into the field in a way a real keyboard doesn't — for example, without the same per-keystroke key events or the same focus/blur timing — and that difference is enough to desync the Angular form control from the picker's internal state in a way an actual user typing would never trigger. This is recorded here, rather than silently deleted, both for transparency about the correction and because the underlying screenshots and DOM inspection are still accurate records of what the automated tool itself did — they just don't demonstrate a real, user-facing bug. If a genuine save-failure edge case does exist under some specific real-world input pattern neither of us has hit yet, it would need to be found through manual testing, not this tooling.
- **Session interruption during re-verification:** partway through investigating the discrepancy above, the authenticated Chrome session unexpectedly landed on the login screen. Rather than attempting to sign back in on the user's behalf (which risks acting on credentials or an OAuth flow that isn't this session's to complete), automated testing was stopped at that point. If further live re-testing is wanted, it would need to resume after the user signs back in themselves.
- **Testing-methodology limitation, not a product finding:** after receiving explicit user approval to click "Submit Orders" and then "Continue" on the resulting Season Minimum dialog (with instructions to cancel rather than finalize), the "Continue" click specifically was blocked by this session's own automated safety guardrail, independent of the user's approval. No workaround was attempted, per that guardrail's own instruction; "Go back" was used instead to exit safely with no state change. This means checkout testing in this case stops one screen short of wherever "Continue" would have led (very likely a final confirm/place-order screen) — that next screen remains unobserved and would need to be evaluated some other way (for example, screen-sharing or a live walkthrough with the UX Assessment Lead directly) rather than through this tool's own automated browsing.
- The X-Warehouse date calendar's enabled-day range appeared, across three attempts, to stay anchored near the current date rather than reactively updating after a Start Date change made earlier in the same panel/reopen cycle — flagged with lower confidence, since it may be an artifact of this session's own JS-driven test interaction rather than genuine end-user behavior. Not pursued to a fully conclusive resolution, to avoid over-spending time on an ambiguous, hard-to-reproduce thread at the expense of this case's broader scope; worth a dedicated, careful re-check using only real mouse-driven interaction (not JS-dispatched events) before treating it as a confirmed bug.
- Finding 2 (the per-color minimum-quantity indicator) was left incomplete on desktop; the tablet retest resolved it fully — see the updated Finding 2 above (threshold confirmed at 4 units, tooltip text confirmed, does not block checkout).
- As in Case 07, "Long Sleeve Crew" (PA1182) remains favorited in all 5 colors at quantity 0 in the shared Fall 2026 cart — pre-existing residual test data from Case 06/07, not created or touched in this case, and still not cleaned up pending a decision with the UX Assessment Lead (see Case 07's own Notes).
- **Tablet retest (768px) completed** using this session's built-in browser pane (Claude in Chrome was unavailable after the desktop-testing logout incident). All desktop findings and positive observations were confirmed to carry over unchanged; Finding 2 was additionally resolved from incomplete to confirmed (see above). One tooling-only artifact was found and resolved during this pass — the built-in browser's `computer` tool's screenshot-to-CSS-pixel coordinate ratio at this tablet preset measured empirically at ~1.094×, not the ~1.042× a naive 800/768 and 1066/1024 calculation would suggest — recorded above in the tablet Steps section as a caution for future retests with this tool, since using the wrong ratio can look identical to a genuine silent-click-failure bug until checked against a real, listener-confirmed click coordinate. One new, unresolved question surfaced at the checkout checkpoint: clicking "Submit Orders" here triggered a "SELECT CUSTOMER" prompt that never appeared on desktop, most likely a session/account-context difference rather than a tablet-specific behavior — not investigated further, exited via "CANCEL" per the scope of approval given. No screenshots were saved for this tablet pass: the built-in browser's `computer` screenshot action returns images inline only, with no equivalent to the auto-saved temp-file mechanism the Chrome extension tool used for desktop's `01`–`17` screenshots, and no alternative save-to-disk path was found — this section is written from direct observation and DOM/network verification instead (as the "Steps" prose above documents in detail), but lacks the visual record the rest of this case has.
- **Mobile retest (375px) attempted, not completed — case closed.** Once a customer was selected in this session's built-in browser, a mobile retest was attempted there directly: every click timed out (~30s), even on trivial elements, with no clear cause found — not a stale tab or a broader tooling outage, since a freshly created tab showed the same symptom, and a device/connectivity check responded normally. Retried at the UX Assessment Lead's suggestion once they confirmed they weren't using the browser themselves — same result. Tried Claude in Chrome next, at the UX Assessment Lead's suggestion; confirmed empirically that its window-resize tool does not produce a genuine 375px mobile viewport (the page kept rendering at full desktop width regardless), so that path was abandoned before it could even test the real question. Moved to a collaborative workflow instead, with the UX Assessment Lead performing each click directly on their own machine: the first click (opening a test style's drawer) worked and surfaced a new "Delivery" dropdown not seen on desktop or tablet, but the next click — on that dropdown — did not respond either through this session's tooling or through the UX Assessment Lead's own direct click. Because the UX Assessment Lead's own manual click also failed, this points to a genuine site or environment condition at the time of testing, not a limitation specific to any one testing approach. At that point the UX Assessment Lead decided to close the case here rather than keep investigating: no cart test data was added at mobile width beyond opening that one drawer, so no cleanup was needed. This case is closed with desktop and tablet (768px) coverage confirmed; mobile (375px) coverage was attempted but not completed.
